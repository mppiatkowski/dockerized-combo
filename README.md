Repo for running frontend and backend

It uses docker-compose to set up a local environment

```
docker-compose up --build
```

In /frontend there is FE: React project
In /backend there is BE: JAVA project
